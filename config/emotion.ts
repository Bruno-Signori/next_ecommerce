import styled, {CreateStyled} from '@emotion/styled';

import theme from '../styles/theme';


export default styled as CreateStyled<typeof theme>;

// esse documneto é feito para iserrirmos as nossas configuraçoes
